% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graft-tips.R
\name{apply_grafts_from_table}
\alias{apply_grafts_from_table}
\title{Apply grafting plan to a tree from a parameter table}
\usage{
apply_grafts_from_table(
  tree,
  plan_df,
  default_shape1 = 1,
  default_shape2 = 1,
  default_min_frac = 0.4,
  default_max_frac = 0.6,
  default_include_terminal = TRUE,
  default_include_stem = FALSE,
  seed = NULL,
  strict = TRUE,
  return_log = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{tree}{A \code{phylo} object.}

\item{plan_df}{Data frame with at least columns: \code{Function}, \code{GraftedTip}, \code{Sister}.}

\item{default_shape1, default_shape2}{Beta-shape parameters for randomization (if used by graft funcs).}

\item{default_min_frac, default_max_frac}{Range on edge where to attach.}

\item{default_include_terminal, default_include_stem}{Flags for within-clade grafting.}

\item{seed}{Optional integer seed.}

\item{strict}{If \code{TRUE}, stop on first error; otherwise continue with warnings.}

\item{return_log}{If \code{TRUE}, return list with \code{tree} and \code{log}; else just the tree.}

\item{verbose}{Logical. If \code{TRUE}, print progress and diagnostic messages
while applying grafting operations.}
}
\value{
\code{list(tree=phylo, log=data.frame)} or \code{phylo}.
}
\description{
\strong{Grafting modes}
}
\details{
The \code{Function} column in the plan controls how taxa are grafted:

\itemize{
\item \code{"sister_to_tip"}:
Attach the new taxon as a sister to a specified tip.

\item \code{"sister_to_clade"}:
Attach the new taxon as sister to a clade defined by one or more tips.

\item \code{"within_clade_random"}:
Insert the new taxon at a random position within a clade,
optionally constrained by Beta-distributed branch position priors.
}

\strong{Branch position priors}

When placing a taxon along a branch, its position is sampled from a
Beta distribution defined by \code{shape1} and \code{shape2},
and constrained to lie between \code{min_frac} and \code{max_frac}
of branch length.
}
