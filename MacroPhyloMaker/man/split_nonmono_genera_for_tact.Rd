% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tact-wrapper.R
\name{split_nonmono_genera_for_tact}
\alias{split_nonmono_genera_for_tact}
\title{Split non-monophyletic genera into temporary pseudo-genera for TACT}
\usage{
split_nonmono_genera_for_tact(
  tree,
  tax,
  nonmono = c("split", "skip", "error"),
  nonmono_allocation = c("proportional", "equal", "random"),
  exclude_tips = character(0),
  seed = 1,
  logger = NULL,
  biogeo = FALSE,
  realm_col = "realm",
  biogeo_unknown_realm = "Unknown",
  biogeo_apply_to_all_genera = TRUE
)
}
\arguments{
\item{tree}{An object of class \code{phylo}.}

\item{tax}{Taxonomy data frame.}

\item{nonmono}{One of \code{"split"}, \code{"skip"}, or \code{"error"}.}

\item{nonmono_allocation}{Missing-species allocation among pseudo-genera:
\code{"proportional"}, \code{"equal"}, or \code{"random"}.}

\item{exclude_tips}{Character vector of backbone tips to protect from grafting.}

\item{seed}{Integer random seed.}

\item{logger}{Optional logger object.}

\item{biogeo}{Logical. If \code{TRUE}, preferentially allocate missing species to
temporary anchors assigned to the same biogeographic realm.}

\item{realm_col}{Character. Name of the taxonomy column containing
biogeographic realm assignments. If absent, the column is created and
populated with \code{biogeo_unknown_realm}.}

\item{biogeo_unknown_realm}{Character. Label used for missing or unknown
realm assignments.}

\item{biogeo_apply_to_all_genera}{Logical. If \code{TRUE} and \code{biogeo = TRUE},
create realm-aware singleton anchors for all genera represented in the
backbone that also have missing species in the taxonomy, rather than only
for genera detected as non-monophyletic.}
}
\value{
A list with \code{tree}, \code{taxonomy}, \code{map}, \code{nonmono}, and \code{skipped}.
}
\description{
Non-monophyletic genera cannot be safely represented as a single taxonomic
unit for TACT grafting. This helper temporarily renames separate backbone
occurrences of a non-monophyletic genus as pseudo-genera such as
\code{TACTTMPNeoponera01}. Protected excluded tips are renamed to \code{TACTEXCL...}
pseudo-genera and receive no missing species. After TACT, temporary labels are
removed by \code{\link[=restore_tact_temp_names]{restore_tact_temp_names()}}.
}
