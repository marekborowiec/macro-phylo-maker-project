% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tact-wrapper.R
\name{prepare_tact_backbone_labels}
\alias{prepare_tact_backbone_labels}
\title{Prepare backbone labels for TACT}
\usage{
prepare_tact_backbone_labels(
  tree,
  tax,
  genus_only = c("replace_random_species", "temporary_species", "keep"),
  species_code = c("keep", "temporary_species", "drop"),
  sample_with_replacement = TRUE,
  fallback_prefix = "sp",
  seed = 1,
  logger = NULL
)
}
\arguments{
\item{tree}{An object of class \code{phylo}.}

\item{tax}{Taxonomy data frame from \code{\link[=read_tact_taxonomy]{read_tact_taxonomy()}}.}

\item{genus_only}{One of \code{"replace_random_species"}, \code{"temporary_species"},
or \code{"keep"}.}

\item{species_code}{One of \code{"keep"}, \code{"temporary_species"}, or \code{"drop"}.}

\item{sample_with_replacement}{Logical. Whether genus-only tips can sample an
already-used species if no unused valid species remain.}

\item{fallback_prefix}{Character prefix for temporary species labels.}

\item{seed}{Integer random seed.}

\item{logger}{Optional logger object.}
}
\value{
A list with \code{tree}, \code{taxonomy}, \code{label_map}, and \code{dropped} elements.
}
\description{
Handles genus-only tips, code-like epithets, and backbone tips that are absent
from the taxonomic table. Code-like tips are kept by default and added to the
taxonomy as known terminals so TACT does not treat them as missing.
}
