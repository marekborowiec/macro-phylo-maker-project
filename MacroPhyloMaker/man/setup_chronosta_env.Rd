% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chronosta-utils.R
\name{setup_chronosta_env}
\alias{setup_chronosta_env}
\title{Set up the Python environment and script for ChronoSTA}
\usage{
setup_chronosta_env(
  python = NULL,
  install = TRUE,
  download = TRUE,
  dest_dir = chronosta_default_dir()
)
}
\arguments{
\item{python}{Character. Optional path or command name for a Python
executable. If \code{NULL}, \code{find_python()} searches for \code{python3} and then
\code{python} on the system path.}

\item{install}{Logical. If \code{TRUE}, install Biopython, pandas, NumPy, SciPy,
and Matplotlib into the environment associated with \code{python} using
\verb{python -m pip install}.}

\item{download}{Logical. If \code{TRUE}, download \code{chronosta.py} into \code{dest_dir}.
If \code{FALSE}, use \code{find_chronosta_script()} to locate an existing script
without downloading it.}

\item{dest_dir}{Character. Directory in which \code{chronosta.py} is stored when
\code{download = TRUE}. Defaults to the user-writable ChronoSTA directory
returned by \code{chronosta_default_dir()}.}
}
\value{
Invisibly returns a list with two elements: \code{python}, the resolved
Python executable path, and \code{chronosta_script}, the resolved path to
\code{chronosta.py}.
}
\description{
Prepares the external Python components required by the ChronoSTA-enabled
grafting workflow. The function can install the required Python packages,
download \code{chronosta.py}, and verify that the selected Python executable can
locate all required dependencies.
}
\details{
The function operates on the Python environment associated with the selected
executable. It does not activate that environment or permanently configure R.
Use the same Python executable for setup, verification with
\code{\link[=check_chronosta_python]{check_chronosta_python()}}, and \code{\link[=run_chronosta_grafting]{run_chronosta_grafting()}}.

Set \code{install = FALSE} when the Python dependencies are already installed. Set
\code{download = FALSE} only when an existing \code{chronosta.py} can be located by
\code{find_chronosta_script()}.
}
\examples{
\dontrun{
# Automatically locate Python, install dependencies, and download ChronoSTA.
env <- setup_chronosta_env()

# Use a project-specific virtual environment on Unix-like systems.
env <- setup_chronosta_env(
  python = ".venv-chronosta/bin/python"
)

# Native Windows example.
env <- setup_chronosta_env(
  python = ".venv-chronosta/Scripts/python.exe"
)

# Verify an environment without reinstalling its Python packages.
env <- setup_chronosta_env(
  python = ".venv-chronosta/bin/python",
  install = FALSE
)
}

}
