% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-tact-grafting-replicates.R
\name{run_tact_grafting_replicates}
\alias{run_tact_grafting_replicates}
\title{Run multiple TACT grafting replicates}
\usage{
run_tact_grafting_replicates(n_replicates, seed_start = 1, out_prefix, ...)
}
\arguments{
\item{n_replicates}{Integer. Number of TACT grafting replicates to run.}

\item{seed_start}{Integer. Seed used for the first replicate. Subsequent
replicates use consecutive integer seeds generated with \code{\link[base:seq]{base::seq.int()}}.}

\item{out_prefix}{Character. Common output prefix. A replicate identifier is
appended automatically to this prefix for each call to
\code{\link[=run_tact_grafting]{run_tact_grafting()}}. The manifest is written using the unmodified common
prefix.}

\item{...}{Additional arguments passed to \code{\link[=run_tact_grafting]{run_tact_grafting()}}. Do not pass
\code{seed} or \code{out_prefix} through \code{...}, because this function supplies those
arguments separately for each replicate.}
}
\value{
An object of class \code{tact_grafting_replicates}, represented as a list
with three elements:
\describe{
\item{results}{A list containing the result returned by each call to
\code{\link[=run_tact_grafting]{run_tact_grafting()}}.}
\item{manifest}{A data frame recording replicate numbers, seeds, and
principal output paths.}
\item{manifest_file}{The path to the tab-delimited replicate manifest.}
}
}
\description{
Runs \code{\link[=run_tact_grafting]{run_tact_grafting()}} repeatedly using sequential random seeds and
replicate-specific output prefixes. The function writes a tab-delimited
manifest linking each replicate and seed to its principal tree and validation
files.
}
\details{
Replicate output prefixes are generated by appending \verb{_rep001}, \verb{_rep002},
and so forth to \code{out_prefix}.

Replicate seeds are generated as consecutive integers beginning with
\code{seed_start}. The manifest is written to
\verb{<out_prefix>_replicate_manifest.tsv} and includes paths to the cleaned tree,
realm-labelled trees, validation reports, taxonomy mismatch reports, and raw
TACT output for each replicate.

The stochastic result of each run depends on the seed and on the arguments
supplied to \code{\link[=run_tact_grafting]{run_tact_grafting()}}. For stronger wrapper-level randomization,
use \code{nonmono_allocation = "random"}.
}
\examples{
\dontrun{
res <- run_tact_grafting_replicates(
  n_replicates = 10,
  seed_start = 1,
  out_prefix = "project/results/tact/Formicidae_complete",
  backbone_tree = "project/results/grafted/final_tree.tre",
  taxonomy = "project/tables/antwiki-valid-species.txt",
  taxonomy_format = "antwiki",
  tact_runner = "docker",
  docker_image = "jonchang/tact",
  nonmono = "split",
  nonmono_allocation = "random",
  biogeo = TRUE,
  country_realm_map = "project/tables/country_udvardy_realm.tsv",
  write_biogeo_labelled_trees = FALSE,
  keep_temp = FALSE
)

res$manifest
res$manifest_file
}

}
