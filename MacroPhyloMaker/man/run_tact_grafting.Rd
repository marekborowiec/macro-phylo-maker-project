% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tact-wrapper.R
\name{run_tact_grafting}
\alias{run_tact_grafting}
\title{Run TACT taxonomy-based grafting for a MacroPhyloMaker backbone}
\usage{
run_tact_grafting(
  backbone_tree,
  taxonomy,
  out_prefix,
  family = "Formicidae",
  taxonomy_format = c("antwiki", "simple", "tact_csv"),
  taxonomy_output = c("newick", "csv"),
  tact_runner = c("docker", "system", "none"),
  docker_image = "jonchang/tact",
  tact_bin = "tact_add_taxa",
  outgroups = NULL,
  seed = 42,
  nonmono = c("split", "skip", "error"),
  nonmono_allocation = c("proportional", "equal", "random"),
  genus_only = c("replace_random_species", "temporary_species", "keep"),
  species_code = c("keep", "temporary_species", "drop"),
  drop_code_species_after_tact = TRUE,
  enforce_taxonomy_tip_count = FALSE,
  biogeo = FALSE,
  country_realm_map = NULL,
  biogeo_unknown_realm = "Unknown",
  biogeo_apply_to_all_genera = TRUE,
  write_biogeo_labelled_trees = TRUE,
  exclude_taxa = NULL,
  exclude_mrca = NULL,
  exclude_missing = c("warn", "error", "ignore"),
  sample_with_replacement = TRUE,
  fallback_prefix = "sp",
  extra_tact_args = character(0),
  keep_temp = TRUE,
  logger = NULL
)
}
\arguments{
\item{backbone_tree}{Character path to a Newick backbone tree, or a \code{phylo}
object.}

\item{taxonomy}{Character path to a taxonomy file, or a data frame.}

\item{out_prefix}{Character output prefix. The wrapper writes audit files and
a TACT work directory using this prefix.}

\item{family}{Character family/root label for taxonomy rows.}

\item{taxonomy_format}{One of \code{"antwiki"}, \code{"simple"}, or \code{"tact_csv"}.}

\item{taxonomy_output}{Currently retained for compatibility. The recommended
path always writes CSV and lets TACT build the taxonomy tree.}

\item{tact_runner}{One of \code{"docker"}, \code{"system"}, or \code{"none"}. Use \code{"none"}
to prepare files without running TACT.}

\item{docker_image}{Docker image name for TACT.}

\item{tact_bin}{Local executable name or path for \code{tact_add_taxa}.}

\item{outgroups}{Optional character vector of outgroup taxa to pass to TACT.}

\item{seed}{Integer random seed for stochastic preprocessing decisions.}

\item{nonmono}{One of \code{"split"}, \code{"skip"}, or \code{"error"}, controlling treatment
of non-monophyletic genera.}

\item{nonmono_allocation}{One of \code{"proportional"}, \code{"equal"}, or \code{"random"},
controlling allocation of missing species among pseudo-genera.}

\item{genus_only}{One of \code{"replace_random_species"}, \code{"temporary_species"}, or
\code{"keep"}, controlling genus-only backbone tips.}

\item{species_code}{One of \code{"keep"}, \code{"temporary_species"}, or \code{"drop"},
controlling code-like labels such as \code{Eburopone_MG04}.}

\item{drop_code_species_after_tact}{Logical. If \code{TRUE}, remove code-like and \verb{_sp} scaffold terminals from the final TACT tree after grafting is complete.}

\item{enforce_taxonomy_tip_count}{Logical. If \code{TRUE}, stop on a final
tree/taxonomy tip-count mismatch. If \code{FALSE} (default), issue a warning,
write mismatch reports, and return the full tree.}

\item{biogeo}{Logical. If \code{TRUE}, use type-locality country mapped to
biogeographic realm to allocate missing species preferentially to same-realm
temporary genus anchors.}

\item{country_realm_map}{Optional path or data frame mapping \code{Country} to
\code{UdvardyRealm} or \code{Realm}.}

\item{biogeo_unknown_realm}{Character label for missing or unmapped realm.}

\item{biogeo_apply_to_all_genera}{Logical. If \code{TRUE}, realm-aware singleton
anchors are created for all genera with backbone representatives and missing
species, not only genera detected as non-monophyletic.}

\item{write_biogeo_labelled_trees}{Logical. If \code{TRUE}, write realm-appended
input/backbone and final cleaned trees for visual inspection.}

\item{exclude_taxa}{Optional character vector of exact taxa to protect from
grafting. If present in the backbone, the corresponding branch is renamed to
a protected \code{TACTEXCL...} pseudo-genus. If absent from the backbone but
present in taxonomy, it is removed from TACT taxonomy.}

\item{exclude_mrca}{Optional clade exclusions. Supply a character vector of
tip labels or a list of such vectors; each vector is resolved to an MRCA and
all descendant tips are protected.}

\item{exclude_missing}{One of \code{"warn"}, \code{"error"}, or \code{"ignore"}, controlling
behavior when \code{exclude_taxa} are not found in the backbone.}

\item{sample_with_replacement}{Logical. Whether genus-only labels can sample
an already-used species if no unused species remain.}

\item{fallback_prefix}{Character prefix for temporary placeholder species.}

\item{extra_tact_args}{Character vector of additional arguments passed to
\code{tact_add_taxa}.}

\item{keep_temp}{Logical. Whether to keep the TACT work directory after a
successful run.}

\item{logger}{Optional logger object.}
}
\value{
Invisibly returns a list of class \code{tact_grafting_result} containing
the final tree, prepared tree and taxonomy, audit tables, paths, and
parameters.
}
\description{
Prepares a backbone tree and taxonomy for TACT, optionally handles
non-monophyletic genera by splitting them into temporary pseudo-genera,
protects excluded taxa or clades from receiving grafted species, runs TACT via
Docker or a system executable, and restores temporary names in the final tree.
}
\details{
TACT itself is not distributed with MacroPhyloMaker. This function only
prepares inputs and calls external TACT commands.
}
\examples{
\dontrun{
res <- run_tact_grafting(
  backbone_tree = "project/results/grafted/final_tree.tre",
  taxonomy = "project/tables/antwiki-valid-species-8Mar2026.txt",
  out_prefix = "project/results/tact/Formicidae_complete_tact",
  taxonomy_format = "antwiki",
  tact_runner = "docker",
  docker_image = "jonchang/tact",
  outgroups = NULL,
  nonmono = "split",
  exclude_taxa = "Neoponera_bucki"
)
}
}
